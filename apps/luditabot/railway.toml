[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/luditabot/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 60
overlapSeconds = 0
drainingSeconds = 0
