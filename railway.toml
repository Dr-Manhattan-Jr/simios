[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/ciclobot/Dockerfile"

[deploy]
# Long-polling worker — no HTTP port, no healthcheck.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
